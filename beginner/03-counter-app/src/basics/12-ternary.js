
const active = true;

// let msg = '';

// if ( active ) {
//   msg = 'Active';
// } else {
//   msg = 'Inactive';
// }

// const msg = ( active ) ? 'Active' : 'Inactive';
// const msg = ( active ) ? 'Active' : null;

const msg = ( active ) && 'Active';


console.log(msg);
